def bucle_3():
    n_tabla=input("¿que tabla desea vusted que le uestre a continuación: ? ")
    #i es la variable contadora
    for i in range(0,11):
        print str(n_tabla)+ " x "+ i +" = "+str(n_tabla*i)

bucle_3()
